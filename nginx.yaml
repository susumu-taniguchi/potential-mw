apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: potential-app-1
spec:
  containers:
  - name: nginx
    image: nginx:1.21.4
    imagePullPolicy: IfNotPresent
    ports:
    - containerPort: 80
